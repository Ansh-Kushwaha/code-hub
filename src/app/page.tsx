import { Button } from "@/components/ui/button";

export default function Home() {
  return (
    <main className="p-5 min-h-screen">
      <Button variant="outline" size="lg">
        <span>Goto Editor Page</span>
      </Button>
    </main>
  );
}
